import { StateBrasilPipe } from './state-brasil.pipe';

describe('StateBrasilPipe', () => {
  it('create an instance', () => {
    const pipe = new StateBrasilPipe();
    expect(pipe).toBeTruthy();
  });
});
